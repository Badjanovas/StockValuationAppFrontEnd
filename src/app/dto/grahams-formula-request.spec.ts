import { GrahamsFormulaRequest } from './grahams-formula-request';

describe('GrahamsFormulaRequest', () => {
  it('should create an instance', () => {
    expect(new GrahamsFormulaRequest()).toBeTruthy();
  });
});
