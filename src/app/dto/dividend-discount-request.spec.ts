import { DividendDiscountRequest } from './dividend-discount-request';

describe('DividendDiscountRequest', () => {
  it('should create an instance', () => {
    expect(new DividendDiscountRequest()).toBeTruthy();
  });
});
