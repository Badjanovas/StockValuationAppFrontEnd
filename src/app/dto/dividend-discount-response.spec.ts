import { DividendDiscountResponse } from './dividend-discount-response';

describe('DividendDiscountResponse', () => {
  it('should create an instance', () => {
    expect(new DividendDiscountResponse()).toBeTruthy();
  });
});
